{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Liste des articles</h1>
        {% if articles %}
        <div class="list-group">
            {% for article in articles %}
            <a href="{{ path('article_show', {'id': article.id}) }}" class="list-group-item list-group-item-action">{{ article.title }}</a>
            {% endfor %}
        </div>
        {% else %}
        <p>Aucun article à afficher</p>
        {% endif %}
    </div>
{% endblock %}